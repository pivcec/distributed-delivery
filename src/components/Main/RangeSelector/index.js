import React from "react";
import styled from "@emotion/styled";

const Container = styled.div({
  flex: 0.25
});

const RangeSelector = () => {
  return (
    <Container>
      <div>range selector</div>
    </Container>
  );
};

export default RangeSelector;
