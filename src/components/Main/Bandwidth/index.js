import React from "react";
import styled from "@emotion/styled";

const Container = styled.div({
  flex: 1
});

const Bandwidth = () => {
  return (
    <Container>
      <div>capacity offload</div>
    </Container>
  );
};

export default Bandwidth;
