import React from "react";
import styled from "@emotion/styled";

const Container = styled.div({
  flex: 0.25
});

const TrafficSelector = () => {
  return (
    <Container>
      <div>traffic selector</div>
    </Container>
  );
};

export default TrafficSelector;
